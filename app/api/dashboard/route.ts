/**
 * En esta ruta se manejan las solicitudes GET para obtener los datos del dashboard.
 * El admin (dueño del complejo) puede ver las canchas que tiene reservadas en el dia por hora y elegir el dia que desea ver.
 * Si tiene mas de un complejo, puede elegir el complejo del que quiere ver el dashboard.
 */

export async function GET(request: Request) {}
